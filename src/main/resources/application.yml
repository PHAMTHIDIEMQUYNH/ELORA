server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/CosmeticEcommerce?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

        connection:
          characterEncoding: utf8mb4
          CharSet: utf8mb4
          useUnicode: true
